/**
 * components/ui/Toast.jsx
 * Re-export of the ToastContainer so it lives under components/ui/
 * and can also be imported as:
 *   import Toast from '../components/ui/Toast'
 */
export { default } from '../ToastContainer'
